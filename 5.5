print(sinh viên:le trung quan MSSV:"235752021610009")
    return sorted(danh_sach)

def tim_phan_tu_lon_nhat(danh_sach):
    """Hàm tìm phần tử lớn nhất trong danh sách."""
    return max(danh_sach)

def tim_phan_tu_nho_nhat(danh_sach):
    """Hàm tìm phần tử nhỏ nhất trong danh sách."""
    return min(danh_sach)
# main.py


import list_utils

input_string = input("Nhập danh sách các số (cách nhau bởi dấu phẩy): ")

danh_sach_so = list(map(int, input_string.split(',')))

danh_sach_sap_xep = list_utils.sap_xep_danh_sach(danh_sach_so)

phan_tu_lon_nhat = list_utils.tim_phan_tu_lon_nhat(danh_sach_sap_xep)
phan_tu_nho_nhat = list_utils.tim_phan_tu_nho_nhat(danh_sach_sap_xep)

print(f"Danh sách sau khi sắp xếp: {danh_sach_sap_xep}")
print(f"Phần tử lớn nhất là: {phan_tu_lon_nhat}")
print(f"Phần tử nhỏ nhất là: {phan_tu_nho_nhat}")
